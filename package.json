{
  "name": "conjone",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "server": "bun run src/server.ts",
    "worker": "bun run src/worker.ts",
    "menu": "bun run src/index.ts",
    "dev": "lsof -ti:3000 | xargs kill -9 || true; bun run src/server.ts & bun run src/worker.ts & sleep 2 && bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/crypto-js": "^4.2.2",
    "@types/qrcode-terminal": "^0.12.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@whiskeysockets/baileys": "^7.0.0-rc.9",
    "dotenv": "^17.3.1",
    "inquirer": "^9.0.0",
    "pino": "^10.3.1",
    "qrcode-terminal": "^0.12.0"
  }
}
